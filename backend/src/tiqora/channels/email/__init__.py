"""Inbound mail pipeline (postmaster) — Phase 4a, feature-flagged off by default.

See ``tiqora.domain.settings_store.KEY_POSTMASTER_ENABLED`` and
``docs/parallel-operation.md`` → "Taking over mail processing".
"""
