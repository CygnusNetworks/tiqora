# Integration / golden-master tests

Cross-package tests (testcontainers MariaDB + PostgreSQL, golden-master diffs
against a real Znuny container) will live here.

Unit tests for the backend package are under `backend/tests/`.
